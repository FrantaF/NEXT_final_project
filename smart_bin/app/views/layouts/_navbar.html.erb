
    <nav class="navbar navbar-expand-lg <% if controller_name == "static" %>fixed-top <% end %><% if controller_name != "static" %>navbar-shrink<% end %>" id="mainNav">
      <div class="container">
        <%= link_to "Binberries", root_path, class: "navbar-brand js-scroll-trigger" %>   
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          Menu
          <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav text-uppercase ml-auto">
            <% if !user_signed_in? %>
                <li class="nav-item">
                  <a class="nav-link js-scroll-trigger" href="#solutions">Home</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link js-scroll-trigger" href="#subscriptions">Subscriptions</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link js-scroll-trigger" href="#contact">Contact</a>
                </li>
                <li class="nav-item">
                  <%= link_to "Client Login", "/sign_in", class: "nav-link"%> 
                </li>
            <% else %>
                <li class="nav-item">         
                  <%= link_to "Dashboard", users_path, class: "nav-link"%>
                </li>
                <% if current_user.admin? %>
                <li class="nav-item">
                  <%= link_to "Admin", rails_admin_path, class: "nav-link" %>
                </li>
                <% end %>
                <li class="nav-item">
                  <%= link_to "Sign Out", "/sign_out", method: :delete, class: "nav-link"%>   
                </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>